apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "complete-production-e2e-dinesh-mistry.fullname" . }}-test-connection"
  labels:
    {{- include "complete-production-e2e-dinesh-mistry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "complete-production-e2e-dinesh-mistry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
